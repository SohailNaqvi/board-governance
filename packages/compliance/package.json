{
  "name": "@ums/compliance",
  "version": "0.1.0",
  "description": "Compliance rules catalog, predicate language, and evaluation engine for ASRB",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest --passWithNoTests",
    "test:unit": "vitest run --reporter=verbose --passWithNoTests"
  },
  "dependencies": {
    "@ums/domain": "workspace:*",
    "@ums/source-data": "workspace:*",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  }
}
