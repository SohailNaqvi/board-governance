{
  "name": "board-governance",
  "version": "0.1.0",
  "description": "Board Governance Module for University DSS",
  "private": true,
  "license": "MIT",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "test:unit": "turbo run test:unit",
    "test:contract": "turbo run test:contract",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test:e2e": "npx playwright test",
    "db:migrate": "cd prisma && prisma migrate dev",
    "db:seed": "cd prisma && ts-node seed.ts && cd .. && npx tsx prisma/seed-compliance-rules.ts",
    "db:push": "cd prisma && prisma db push",
    "db:studio": "cd prisma && prisma studio"
  },
  "dependencies": {
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@prisma/client": "5.22.0",
    "@turbo/gen": "^1.12.4",
    "argon2": "0.41.1",
    "prisma": "5.22.0",
    "tsx": "^4.21.0",
    "turbo": "^1.12.4",
    "wait-on": "^9.0.5"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=9.0.0"
  }
}
