/**
 * /dss-prototype — Frozen reference prototype.
 *
 * Renders the original UniversityDSS.jsx (12,097 lines) byte-identically
 * as it existed before DSS-Visual-1 deleted it. This is a specification
 * artefact, not living code. All data inside is mock.
 *
 * The prototype uses its own Tailwind classes and layout. The new design
 * system tokens (scoped to .dss-root) do NOT apply here.
 */

import UniversityDSS from "./UniversityDSS";

export default function DssPrototypePage() {
  return <UniversityDSS />;
}
