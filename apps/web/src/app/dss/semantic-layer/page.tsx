import { SectionPlaceholder } from '@/components/dss/section-placeholder';

export default function SemanticLayerPage() {
  return (
    <SectionPlaceholder
      sectionName="Semantic Layer"
      description="This page will host the semantic layer configuration, mapping business concepts to underlying data models and defining institutional KPI definitions."
    />
  );
}
