import { SectionPlaceholder } from '@/components/dss/section-placeholder';

export default function ResearchPipelinePage() {
  return (
    <SectionPlaceholder
      sectionName="Research Pipeline"
      description="This page will host the research grants and publications pipeline, tracking proposals, active grants, and output metrics across departments."
    />
  );
}
