import { SectionPlaceholder } from '@/components/dss/section-placeholder';

export default function DataLineagePage() {
  return (
    <SectionPlaceholder
      sectionName="Data Lineage"
      description="This page will host the data lineage explorer, documenting how data flows from source systems through transformation layers into DSS views."
    />
  );
}
